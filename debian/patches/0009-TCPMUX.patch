--- xinetd-2.3.14.orig/xinetd/builtins.c
+++ xinetd-2.3.14/xinetd/builtins.c
@@ -603,7 +603,7 @@
    if( SC_IS_INTERNAL( scp ) ) {
       SC_INTERNAL(scp, nserp);
    } else {
-      exec_server(nserp);
+      child_process(nserp);
    }
 }

